class Solution:
    def percentageLetter(self, s: str, letter: str) -> int:
        count = 0
        for char in s:
             if char == letter:
                count += 1
        len_s = len(s)
        percentage = int(count/len_s * 100)
        return percentage
